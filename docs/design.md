# Design-System — sandbox-3

> Teil des Detailkonzepts (**UI-Domäne**, nur bei UI-Projekten). Geschrieben vom `designer`. Bindend für den `coder`; Konformität (Kontrast/Spacing/A11y) prüft der `reviewer` via UI-Pack-Checklist.

## Tokens
<Farbe (inkl. Dark-Mode), Spacing-Skala (8-pt), Typografie.>

## Komponenten-Patterns
<Wiederkehrende UI-Bausteine + Verhalten.>

## Responsive / Breakpoints
<Verhalten je Breite.>

## Accessibility (WCAG 2.1 AA)
<Kontrast (berechnet, nicht geschätzt), sichtbarer Fokus, Tastatur-Navigation, Touch-Targets ≥ 44–48 px.>
